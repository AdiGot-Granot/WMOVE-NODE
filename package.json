{
  "name": "nodewmove",
  "version": "0.1.0",
  "private": true,
  "description": "wMove — Node.js (Express) rewrite of the Visual FoxPro + West Wind Web Connection application. Phase 1: behavior-preserving migration against the existing GMOVE SQL Server database.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node test/smoke.js",
    "catalog": "node scripts/build-endpoint-catalog.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "mssql": "^11.0.1",
    "mustache-express": "^1.3.2",
    "pino": "^9.3.2",
    "pino-http": "^10.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
